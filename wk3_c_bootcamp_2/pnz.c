#include <stdio.h>

int main() {
    int num = -80;

    if (num >0)
    {
        printf("Number is positive\n");
    }
    else if (num <0)
    {
        printf("Number is negative\n");
    }
    else if (num == 0)
    {
        printf("Number is 0\n");
    }
    return(0);
}